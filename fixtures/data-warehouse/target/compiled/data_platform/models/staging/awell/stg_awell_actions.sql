select 
    id as action_id,
    name as action_name,
    object_name,
    definition_id,
    object_definition_id,
    object_type,
    care_flow_definition_id,
    care_flow_id,
    track_id,
    step_id,
    date(started_at) as action_started_at,
    date(completed_at) as action_completed_at,
    duration_in_seconds,
    date(scheduled_at) as action_scheduled_at,
    indirect_object_id,
    indirect_object_type,
    indirect_object_name,
    resolution,
    status,
    date(last_synced_at) as last_synced_at
from source_prod.awell.actions