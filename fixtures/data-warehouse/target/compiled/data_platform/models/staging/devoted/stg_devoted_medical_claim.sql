select
    ClaimNumber as claim_id,
    ClaimLineNumber as claim_line_number,
    ClaimType as claim_type,
    MBI as patient_id,
    memberID as member_id,
    date(SvcFromDate) as claim_start_date,
    date(SvctoDate) as claim_end_date,
    date(claimlineservicedate) as claim_line_start_date,
    date(null) as claim_line_end_date,
    date(AdmissionDate) as admission_date,
    date(DischargeDate) as discharge_date,
    AdmissionSource as admit_source_code,
    AdmissionType as admit_type_code,
    DischargeCode as discharge_disposition_code,
    PlaceOfService as place_of_service_code,
    BillType as bill_type_code,
    nullif(DrgCode, 'Devoted is secondary insurance') as ms_drg_code,
    to_varchar(null) as apr_drg_code,
    RevenueCode as revenue_center_code,
    Units as service_unit_quantity,
    to_varchar(ProcedureCode) as hcpcs_code,
    to_varchar(ProcModifier) as hcpcs_modifier_1,
    to_varchar(ProcModifier2) as hcpcs_modifier_2,
    to_varchar(ProcModifier3) as hcpcs_modifier_3,
    to_varchar(ProcModifier4) as hcpcs_modifier_4,
    to_varchar(null) as hcpcs_modifier_5,
    RenderingProviderNPI as rendering_npi,
    nullif(RenderingProviderTIN, '') as rendering_tin,
    BillingProviderNPI as billing_npi,
    nullif(BillingProviderTIN, '') as billing_tin,
    BillingProviderNPI as facility_npi,
    date(datecreated) as created_date,
    date(DatePaid) as paid_date,
    try_to_number(TotAmtPaid, 38, 8) as paid_amount,
    try_to_number(TotAmtAllowed, 38, 8) as allowed_amount,
    try_to_number(TotAmtChg, 38, 8) as charge_amount,
    try_to_number(TotAmtChg, 38, 8) as total_cost_amount,
    trim(replace(PrincipalDiagnosisCode, '.', '')) as principal_diagnosis_code,
    trim(replace(Icd10Diag1, '.', '')) as icd10diag1,
    trim(replace(Icd10Diag2, '.', '')) as icd10diag2,
    trim(replace(Icd10Diag3, '.', '')) as icd10diag3,
    trim(replace(Icd10Diag4, '.', '')) as icd10diag4,
    trim(replace(Icd10Diag5, '.', '')) as icd10diag5,
    trim(replace(Icd10Diag6, '.', '')) as icd10diag6,
    trim(replace(Icd10Diag7, '.', '')) as icd10diag7,
    trim(replace(Icd10Diag8, '.', '')) as icd10diag8,
    trim(replace(Icd10Diag9, '.', '')) as icd10diag9,
    trim(replace(Icd10Diag10, '.', '')) as icd10diag10,
    trim(replace(Icd10Diag11, '.', '')) as icd10diag11,
    trim(replace(Icd10Diag12, '.', '')) as icd10diag12,
    trim(replace(Icd10Diag13, '.', '')) as icd10diag13,
    trim(replace(Icd10Diag14, '.', '')) as icd10diag14,
    trim(replace(Icd10Diag15, '.', '')) as icd10diag15,
    trim(replace(Icd10Diag16, '.', '')) as icd10diag16,
    trim(replace(Icd10Diag17, '.', '')) as icd10diag17,
    trim(replace(Icd10Diag18, '.', '')) as icd10diag18,
    trim(replace(Icd10Diag19, '.', '')) as icd10diag19,
    trim(replace(Icd10Diag20, '.', '')) as icd10diag20,
    trim(replace(Icd10Diag21, '.', '')) as icd10diag21,
    trim(replace(Icd10Diag22, '.', '')) as icd10diag22,
    trim(replace(Icd10Diag23, '.', '')) as icd10diag23,
    trim(replace(Icd10Diag24, '.', '')) as icd10diag24,
    trim(replace(Icd10Diag25, '.', '')) as icd10diag25,
    trim(replace(Icd10Diag26, '.', '')) as icd10diag26,
    trim(replace(Icd10Diag27, '.', '')) as icd10diag27,
    trim(replace(Icd10Diag28, '.', '')) as icd10diag28,
    trim(replace(Icd10Diag29, '.', '')) as icd10diag29,
    trim(replace(Icd10Diag30, '.', '')) as icd10diag30,
    trim(replace(Icd10Diag31, '.', '')) as icd10diag31,
    trim(replace(Icd10Diag32, '.', '')) as icd10diag32,
    trim(replace(Icd10Diag33, '.', '')) as icd10diag33,
    trim(replace(Icd10Diag34, '.', '')) as icd10diag34,
    trim(replace(Icd10Diag35, '.', '')) as icd10diag35,
    trim(replace(Icd10Diag36, '.', '')) as icd10diag36,
    trim(replace(Icd10Diag37, '.', '')) as icd10diag37,
    trim(replace(Icd10Diag38, '.', '')) as icd10diag38,
    trim(replace(Icd10Diag39, '.', '')) as icd10diag39,
    trim(replace(Icd10Diag40, '.', '')) as icd10diag40,
    trim(replace(Icd10Diag41, '.', '')) as icd10diag41,
    trim(replace(Icd10Diag42, '.', '')) as icd10diag42,
    trim(replace(Icd10Diag43, '.', '')) as icd10diag43,
    trim(replace(Icd10Diag44, '.', '')) as icd10diag44,
    trim(replace(Icd10Diag45, '.', '')) as icd10diag45,
    trim(replace(Icd10Diag46, '.', '')) as icd10diag46,
    trim(replace(Icd10Diag47, '.', '')) as icd10diag47,
    trim(replace(Icd10Diag48, '.', '')) as icd10diag48,
    'Devoted' as data_source,
    split(_ab_source_file_url, '/')[array_size(split(_ab_source_file_url, '/'))-1]::varchar as src_file_name,
    date(ReportDate) as last_update,
    row_number() over (partition by memberID, ClaimNumber, ClaimLineNumber order by date(ReportDate) desc) as _rn,
    min(date(ReportDate)) over (partition by ClaimNumber, ClaimLineNumber) as first_received_date
from airbyte_source_prod.devoted.claims_medical